# Copy to `.env.test` in this directory (see root .gitignore). Vite `loadEnv("test", ...)` loads it when
# running `npm run test` (see test/configs/browsers.config.mts).

# Optional. Used by @toruslabs/ethereum-controllers for default Infura RPC URLs when dependencies load in browser tests.
# Empty is fine for the current unit tests.
VITE_APP_INFURA_PROJECT_KEY=
