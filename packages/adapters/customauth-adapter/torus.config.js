const defaultConfig = {
  cjsBundled: true,
  bundledDeps: [],
  analyzerMode: "disabled",
  compilerOptions: {
    strict: true,
  },
};

module.exports = defaultConfig;
// TODO: Import this file into packages which need it
